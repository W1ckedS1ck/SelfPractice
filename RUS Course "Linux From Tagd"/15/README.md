#v Задание 15:
```sh
# Создать  единый скрипт:
# 0. Весь скрипт целиком публикуем в группе. Текст, если нужен, в виде комментариев.
# 1. Инициализировать переменную DT результатом команды получения даты "$(date)"

DT="$(date)"
# 2. Переменную DT разобрать на составные части командой set.
set -- $DT  
# 3. Объявить и инициализировать полученными значениями переменные:
# WEEK_DAY DAY MON YEAR TIME T_ZONE

WEEK_DAY=$1  
MON=$2  
DAY=$3  
TIME=$4  
T_ZONE=$5  
YEAR=$6 
# 4. Распечатать полученные переменные в виде VAR=var_value
echo "День недели: $WEEK_DAY"
echo "Месяц: $MON"
echo "Число: $DAY"
echo "Время: $TIME"
echo "Часовой пояс: $T_ZONE"
echo "Год: $YEAR"
# 5. Установить для переменной  TIME режим экспорта.
export TIME
# 6. Установить для переменных  DAY  и YEAR целый тип.
declare -i DAY YEAR
# 7. Вывести на экран информацию об объявлениях и значениях всех  вышеуказанных переменных.
declare -p WEEK_DAY DAY MON YEAR TIME T_ZONE
# 8. В домашнем каталоге создать файл вида имякомпьютера_год_месяц_день.log
touch "${HOSTNAME}_${YEAR}_${MON}_${DAY}.log"
# 9. Для проверки вывести результат в виде ls -l имя_файла
ls -l *.log
# 10. Не меняя программы пп1-7 сделать так, чтобы  WEEK_DAY печаталась  большими буквами, а TIME T_ZONE маленькими.
declare -u BIG_WEEK_DAY=$WEEK_DAY
declare -l SM_T_ZONE=$T_ZONE
echo "День недели: $BIG_WEEK_DAY"
echo "Часовой пояс: $SM_T_ZONE"
# 11. Вывести на экран.  WEEK_DAY  T_ZONE как в п.4
echo "День недели: $WEEK_DAY"
echo "Часовой пояс: $T_ZONE"
# 12. Вывести на экран имя скрипта.
echo $0
# 13.  Программно (из самого скрипта) вывести на экран содержимое самого скрипта c нумерацией не пустых строк.
cat -n -b $0
# 14. При запуске перенаправить результат выполнения скрипта  в файл lesson.log. Содержимое опубликовать в группе.
bash 14.sh > lesson.log
# 15* В комментарии предложить способ, как обойтись без переменной DT с сохранением функциональности.
set -- $(date)
# 16* Разобрать TIME на часы, минуты секунды.
set -- $(date)
#echo "$@"
shift 3
echo "Время: $1"
IFS=: 
set -- $1
echo "Ч: $1"
echo "М: $2"
echo "С: $3"
```
