#v Задание 19.
```sh
#1.Задан скрипт.  Переписать его используя:
for ((i=1;i<17;++i));do
  for ((j=13;j>3;j--));do
    echo "$i X $j = $((i*j))"
    [[ $i -eq 2 ]] && break 2
  done
done

#1.1 while
echo "__while__"
i=1
while (( i < 17 )); do
    j=13
    while (( j > 3 )); do
        echo "$i X $j = $((i * j))"
        [[ $i -eq 2 ]] && break 2
        (( j-- ))
    done
    (( ++i ))
done

#1.2 until
echo "_until__"
i=1
until (( i >= 17 )); do
    j=13
    until (( j <= 3 )); do
        echo "$i X $j = $((i * j))"
        [[ $i -eq 2 ]] && break 2 
        (( j-- ))
    done
    (( ++i ))
done

#.3 for in
echo "__for in__"
for i in {1..16}; do
    for j in {13..4}; do
        echo "$i X $j = $((i * j))"
        [[ $i -eq 2 ]] && break 2 
    done
done

#2.Написать скрипт для таблицы умножения в порядке возрастания (результат как в примере (Т)), но используя циклы с убыванием переменной цикла. (9 8...1)
echo "__Таблица Умножения без результата <3__"
for ((i=9; i>=1; i--)); do
    for ((j=9; j>=1; j--)); do
        echo "$i X $j = $((i * j))"
        [[ $i -eq 2 ]] && break 2 
    done
done 

#3*.  Самостоятельно разобраться, что делает следующий скрипт и проставить комментарии к каждой строке.
declare -i I=0 K L                              # Переменные будут числовыми и = 0 
while [[ $I -lt 99 ]];do                        # Выполнять пока и < 99
  ((I++))                                       # Увеличиваем и на 1 (и = и+1)
  K=I/10                                        # К = И поделить на 10 без дроби
  L=I%10                                        # Л = И поделить на 10 и взять остаток
  ((K*L)) &&  echo "$K X $L = $((K*L))"         # Умножаем К на Л и вывод показываем на экран ЕСЛИ УМНОЖЕНИЕ = 0. Блин, не догадался :(
done

#4. Изменить скрипт п3,  используя оператор if, для улучшения читаемости. Заменить ((K*L)) двумя более понятными условиями с объединением по ИЛИ
declare -i I=0 K L
while [[ $I -lt 99 ]]; do
  ((I++))
  K=I/10
  L=I%10
  if [[ $K -ne 0 || $L -ne 0 ]]; then
    echo "$K X $L = $((K*L))"
  fi
done

#5. Изменить скрипт п3, используя оператор continue.
declare -i I=0 K L
while [[ $I -lt 99 ]]; do
  ((I++))
  K=I/10
  L=I%10
  if [[ $((K*L)) -eq 0 ]]; then
    continue
  fi
  echo "$K X $L = $((K*L))"
done

#6.Написать скрипт для создания в переменной VAR числа, состоящего из 20 случайных символов в диапазоне (2-8) Вывести на экран значение VAR
for ((i = 0; i < 20; i++)); do
  VAR=$(echo "$VAR$((RANDOM % 7 + 2))" )
done 
echo -n "Сгенерированное число: $VAR " && echo -n "$VAR" | wc -m

#7.Модифицировать скрипт п6, исключив из числа цифры 4 и 6
NUMBERS=(2 3 5 7 8)
for ((i = 0; i < 20; i++)); do
  VAR1+="${NUMBERS[RANDOM % ${#NUMBERS[@]}]}"
done 
echo -n "Сгенерированное число: $VAR1 " && echo -n "$VAR1" | wc -m
```
